 {% extends 'base.html.twig' %}
{% block body_id 'profile' %}
 {% block main %}

     <h1>Hello, {{ user.fullName }}</h1>
     <a href="{{ path('user_mailbox') }}"><span class="glyphicon glyphicon-envelope">{{ messageCount }}</span> </a>

 {% endblock %}