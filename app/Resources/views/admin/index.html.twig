{% extends 'base.html.twig' %}

{% block main %}

    {% for user in allUsers %}
<a href="{{ path('user_profile', {'id':user.id}) }}">{{ user.fullName }}</a>
        <hr/>
    {% endfor %}
{% endblock %}